services:
  migrate:
    image: ${API_IMAGE:?API_IMAGE must be an immutable digest reference}
    build: null
  api:
    image: ${API_IMAGE:?API_IMAGE must be an immutable digest reference}
    build: null
  web:
    image: ${WEB_IMAGE:?WEB_IMAGE must be an immutable digest reference}
    build: null
